// Este método, de nome sugestivo, 
// ordena os elementos do vetor. 
// A chamada mais simples a essa 
// função não requer parâmetros e 
// ordena no array e ordem crescente. 
// Uma chamada mais complexa recebe 
// uma função de ordenação como parâmetro, 
// esta função define o cálculo a ser 
// considerado na comparação entre dois elementos

var vetor1 = new Array(3, 2, 1, 5, 4)
vetor1.sort()
document.write(vetor1)