// A função unshit funciona semelhante à push, 
// mas o invés de inserir elementos no final do 
// array, os insere no início. Podem ser 
// passados um ou vários elementos como parâmetro

var vetor1 = new Array(-1, 0, 1, 2, 3, 4, 5)
var texto = vetor1.toString()
document.write(texto)